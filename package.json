{
  "name": "dulak",
  "version": "0.1.0",
  "license": "MIT",
  "description": "Full-stack boilerplate: Elysia + bun:sqlite + Inertia v3 (Svelte 5) with auth, roles, SSR, Tailwind v4, migrations, tests and Docker.",
  "keywords": ["dulak", "elysia", "bun", "inertia", "svelte", "sqlite", "tailwind", "boilerplate", "ssr"],
  "repository": {
    "type": "git",
    "url": "https://github.com/maulanashalihin/dulak.git"
  },
  "engines": {
    "bun": ">=1.3.0"
  },
  "private": false,
  "type": "module",
  "module": "src/index.ts",
  "scripts": {
    "dev": "bun --watch src/index.ts",
    "dev:css": "bunx @tailwindcss/cli -i src/client/tailwind.css -o src/client/.tailwind.css --watch",
    "build": "bun run scripts/build.ts",
    "start": "NODE_ENV=production bun run src/index.ts",
    "db:seed": "bun run scripts/seed.ts",
    "typecheck": "svelte-check --tsconfig ./tsconfig.json",
    "test": "bun test --isolate"
  },
  "dependencies": {
    "@inertiajs/svelte": "^3.6.1",
    "elysia": "^1.4.29",
    "svelte": "^5.56.8"
  },
  "devDependencies": {
    "@tailwindcss/cli": "^4.3.3",
    "@types/bun": "^1.2.0",
    "svelte-check": "^4.7.4",
    "tailwindcss": "^4.3.3",
    "typescript": "^5.8.0"
  }
}
